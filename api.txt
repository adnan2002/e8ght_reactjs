I updated the "/sessions" POST return data in Login.jsx

now the request stayed the same but the return is this

c.JSON(http.StatusOK, gin.H{
		"user": userResponse{
			ID:                  user.ID,
			FullName:            user.FullName,
			Email:               user.Email,
			Phone:               user.Phone.String,
			Nationality:         user.Nationality.String,
			DateOfBirth:         user.DateOfBirth.Time.Format(time.DateOnly),
			Gender:              string(user.Gender),
			AvatarUrl:           user.AvatarUrl.String,
			Role:                string(user.Role.UserRole),
			CompletedOnboarding: user.CompletedOnboarding,
			CreatedAt:           user.CreatedAt.Time,
			UpdatedAt:           user.UpdatedAt.Time,
		},
		"access_token": accessToken,
		"expires_in":   int(s.Config.ACCESS_TOKEN_DURATION.Seconds()),
		"default_address": defaultAddress,
	})


so in the login i want you to take the new default_address object and store it in local storage that's it. 